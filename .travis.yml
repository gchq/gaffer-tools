language: java
jdk:
  - oraclejdk8
install: git clone https://github.com/gchq/Gaffer.git && cd Gaffer && git checkout develop && mvn clean install -Pquick
script: mvn install -PreducedMemory -Dfindbugs.skip=true && mvn findbugs:findbugs && mvn javadoc:javadoc
